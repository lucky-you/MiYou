<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="4dp"
    android:orientation="vertical">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/clPropsBackground"
        android:layout_width="match_parent"
        android:layout_height="108dp"
        android:layout_gravity="center_horizontal"
        android:background="@drawable/shape_shop_mall_props_bg"
        android:scaleType="centerCrop">


        <ImageView
            android:id="@+id/ivPropsPhoto"
            android:layout_width="45dp"
            android:layout_height="59dp"
            android:layout_marginTop="10dp"
            android:scaleType="centerCrop"
            android:src="@drawable/shop_mall_header_photo"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/tvPropsPrice"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="10dp"
            android:drawableLeft="@drawable/wallet_diamond_icon"
            android:drawablePadding="4dp"
            android:gravity="center_vertical"
            android:text="500"
            android:textColor="@color/color_333"
            android:textSize="16sp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>

    <TextView
        android:id="@+id/tvPropsName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="6dp"
        android:gravity="center_vertical"
        android:text="哈根达斯"
        android:textColor="@color/color_333"
        android:textSize="15sp" />


    <TextView
        android:id="@+id/tvPropsRemainingTime"
        style="@style/TextViewStyleOf14Sp"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="6dp"
        android:gravity="center"
        android:text="剩余43天"
        android:textSize="12sp"
        android:visibility="gone" />
</LinearLayout>
